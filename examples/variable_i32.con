module main

import "console"

fn main() i32
{
    // a : i32 = 100   // Declare a as a constant i32 set to 100
    // a += 3          // Illegal: cannot add 3 to a constant

    a : var i32 = 100  // Declare a as a variable i32
    a += 3             // Legal: add 3 to the variable a

    console.print("{}", a); // 103

    return 0
}